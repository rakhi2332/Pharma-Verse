{
  "name": "pharmaverse-ai-root",
  "version": "1.0.0",
  "description": "PharmaVerse B.Pharmacy AI Platform Root Workspace",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start": "node backend/server.js",
    "build": "npm run build --prefix frontend"
  }
}
